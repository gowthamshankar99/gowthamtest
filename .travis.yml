sudo: required
language: node_js
service:
 - docker
before_install:
 - apt install httpd -y
script:
 - uname -r
 - ls
 - docker images
 - echo "hello" >> /var/www/html/index.html
 - service httpd start
 - curl http://localhost:80

# gowthamshankar
